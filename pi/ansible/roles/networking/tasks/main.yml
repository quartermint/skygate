---
# Networking role: hostapd WiFi AP, NetworkManager exclusion, nftables firewall/NAT
# Configures wlan1 as a WPA2-PSK access point with firewall and NAT routing

- name: Install networking packages
  ansible.builtin.apt:
    name:
      - hostapd
    state: present

- name: Stop wpa_supplicant for AP interface
  ansible.builtin.systemd:
    name: wpa_supplicant
    state: stopped
    enabled: false
  # absent on images that never ran wpa_supplicant
  failed_when: false

- name: Deploy NetworkManager unmanaged config
  ansible.builtin.template:
    src: unmanaged.conf.j2
    dest: /etc/NetworkManager/conf.d/skygate-unmanaged.conf
    mode: "0644"
  notify: Restart networkmanager
- name: Check if AP interface has static IP
  ansible.builtin.shell: |
    set -o pipefail
    grep -q "{{ ap_gateway }}" <<< "$(ip addr show {{ ap_interface }} 2>/dev/null)"
  args:
    executable: /bin/bash
  register: networking_ap_ip_check
  changed_when: false
  failed_when: false

- name: Configure static IP on AP interface
  ansible.builtin.command: >
    ip addr add {{ ap_gateway }}/24 dev {{ ap_interface }}
  when: networking_ap_ip_check.rc != 0
  changed_when: true

- name: Deploy hostapd configuration
  ansible.builtin.template:
    src: hostapd.conf.j2
    dest: /etc/hostapd/hostapd.conf
    mode: "0600"
  notify: Restart hostapd
- name: Unmask hostapd (Bookworm ships it masked)
  ansible.builtin.systemd:
    name: hostapd
    masked: false

- name: Enable and start hostapd
  ansible.builtin.systemd:
    name: hostapd
    enabled: true
    state: started

- name: Deploy nftables configuration
  ansible.builtin.template:
    src: nftables.conf.j2
    dest: /etc/nftables.conf
    mode: "0644"
  notify: Restart nftables
- name: Enable and start nftables
  ansible.builtin.systemd:
    name: nftables
    enabled: true
    state: started

- name: Configure policy routing for aviation bypass
  ansible.builtin.shell: |
    set -o pipefail
    CHANGED=0
    if ! grep -q "fwmark 0x1" <<< "$(ip rule show)"; then
      ip rule add fwmark 0x1 table 100 priority 100
      CHANGED=1
    fi
    if ! grep -q "default" <<< "$(ip route show table 100)"; then
      ip route add default dev {{ uplink_interface }} table 100
      CHANGED=1
    fi
    echo "changed=$CHANGED"
  args:
    executable: /bin/bash
  register: networking_policy_routing_result
  changed_when: "'changed=1' in networking_policy_routing_result.stdout"

---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart skygate-bypass
  ansible.builtin.systemd:
    name: skygate-bypass
    state: restarted
    daemon_reload: true
